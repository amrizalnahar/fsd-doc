# Audit v2.0.0 — Temuan Adversarial

> Dibuat: 2026-08-06  
> Diperbarui: 2026-08-09 — seluruh temuan (P0 V2-001..V2-003, P1 V2-004..V2-010,
> P2 V2-011) telah diperbaiki (lihat kolom Status per baris). Sebagian
> perbaikan P1 bersifat **instruksi yang diperkuat di SKILL.md** (ditegakkan
> agen saat menjalankan skill), bukan validasi deterministik di kode — dicatat
> eksplisit per baris.

## Ringkasan

| Prioritas | Jumlah | Fokus | Status |
|---|---:|---|---|
| P0 / Tinggi | 3 | Kebocoran data internal, aset DOCX hilang tanpa kegagalan, template DOCX kustom tidak digunakan | **Diperbaiki (2026-08-09)** — kode, diverifikasi otomatis |
| P1 / Menengah | 7 | Konfigurasi mati, konsistensi flow, keamanan kredensial/prompt, dan integritas dokumen | **Diperbaiki (2026-08-09)** — campuran kode (validator, logo) + instruksi SKILL.md yang diperkuat |
| P2 / Rendah | 1 | Validasi warna brand dan kualitas pesan error | **Diperbaiki (2026-08-09)** — kode, diverifikasi otomatis |

## Temuan terkonfirmasi

| ID | Prioritas | Status | Area | Temuan | Skenario gagal / dampak | Bukti | Arah perbaikan |
|---|---|---|---|---|---|---|---|
| V2-001 | P0 | **Diperbaiki** (2026-08-09) | Konversi DOCX | `build-docx.ps1` tidak memangkas blok `<!-- INTERNAL:START -->` … `<!-- INTERNAL:END -->`. Pandoc hanya membuang komentar, bukan isi blok. | Pemakaian langsung `build-docx.ps1` dapat mengirim Matriks Keterlacakan, path sumber, dan nomor baris internal ke klien. Uji audit membuktikan marker internal tetap muncul di `.docx`. | `skills/fsd-convert/docx-kit/build-docx.ps1` (blok pemangkasan sebelum Pandoc); aturan lama hanya ada di `skills/fsd-convert/SKILL.md`. | Script kini mem-preprocess salinan Markdown ke file sementara, memangkas semua blok INTERNAL otomatis, dan gagal (exit 1) bila menemukan penanda START/END tak berpasangan. Diverifikasi: teks "Matriks/Keterlacakan/secret" tidak muncul lagi di `.docx` hasil konversi uji. |
| V2-002 | P0 | **Diperbaiki** (2026-08-09) | Konversi DOCX / aset | Konverter hanya memakai direktori Markdown sebagai `--resource-path`; tidak memvalidasi semua image reference. Warning Pandoc tentang resource hilang tidak membuat proses gagal. | Link gambar yang salah atau aset tidak ditemukan dapat diganti menjadi teks deskripsi; skrip tetap mencetak `[ok] Selesai`. Screenshot, diagram alur, atau ERD klien dapat hilang diam-diam. | `skills/fsd-doc/SKILL.md:239-256`; `skills/fsd-convert/docx-kit/build-docx.ps1` (validasi sebelum Pandoc). | Script kini mem-parse semua `![...](path)` lokal, mengecek eksistensinya relatif terhadap folder Markdown, dan berhenti sebelum memanggil Pandoc dengan daftar path yang hilang bila ada. Diverifikasi: exit 1 + daftar path saat aset hilang; exit 0 saat semua aset ada. |
| V2-003 | P0 | **Diperbaiki** (2026-08-09) | Branding DOCX | `docx.reference` tersedia di config tetapi tidak dibaca oleh skill atau script. Script selalu memakai `docx-kit/reference.docx`. | Proyek yang mengatur template DOCX per klien tetap memperoleh template bawaan engine, sehingga format/branding klien dapat salah. | `skills/fsd-init/doc-fsd.config.example.yml:100-103`; `skills/fsd-convert/docx-kit/build-docx.ps1` (param `-Reference`). | Ditambahkan parameter `-Reference` (fallback ke `reference.docx` bawaan bila kosong, dengan validasi eksistensi file). `SKILL.md` langkah 5 kini menyebutkan resolve `docx.reference` dari config dan meneruskannya lewat `-Reference`. Diverifikasi: `-Reference <path-tidak-ada>` menghasilkan exit 1 dengan pesan yang jelas. |
| V2-004 | P1 | **Diperbaiki** (2026-08-09) — kode | Branding DOCX | `brand.logo` tersedia dan didokumentasikan, tetapi generator reference DOCX tidak membaca atau menyisipkan gambar logo. | Pengguna mengisi path logo valid dan regenerate template, namun cover tidak akan memuat logo. | `skills/fsd-convert/docx-kit/insert-logo.py` (baru); dipanggil dari `build-docx.ps1`; didokumentasikan di `SKILL.md` + `docx-kit/README.md`. | Karena Pandoc mengabaikan body `reference.docx`, logo diimplementasikan sebagai post-process baru (`insert-logo.py`) yang menyisipkan gambar di atas paragraf gaya "Title" hasil konversi. Kosong = tanpa logo; diisi-tapi-hilang = build gagal (exit 1). Diverifikasi end-to-end: image ter-embed di `.docx` (dicek lewat isi `word/media/`) saat logo valid; exit 1 dengan pesan jelas saat path salah. |
| V2-005 | P1 | **Diperbaiki** (2026-08-09) — dokumentasi | Dokumentasi manual | Panduan manual docx-kit meminta penghapusan Daftar Isi manual, tetapi tidak menyebut pemangkasan blok INTERNAL. | Pengguna yang mengikuti README manual dapat menghasilkan ToC ganda dan/atau membocorkan bagian internal. | `skills/fsd-convert/docx-kit/README.md` (bagian "Cara pakai (manual)"). | README kini menjelaskan bahwa `build-docx.ps1` memangkas blok INTERNAL dan memvalidasi gambar secara otomatis, sehingga langkah manual tidak lagi jadi satu-satunya jaring pengaman — sejalan dengan enforcement di script (V2-001/V2-002). |
| V2-006 | P1 | **Diperbaiki** (2026-08-09) — validator + instruksi | Kredensial | `modules[].credentials` bisa menunjuk path bebas untuk dibaca, sementara saat credential belum ada, flow menulis ke path fixed `docs/tasks/credentialRoles/<peran>.md`. `.gitignore` hanya dipastikan untuk folder fixed tersebut. | Credential bisa dibaca dari luar folder aman; lokasi baca dan tulis dapat berbeda; file credential custom berisiko tidak ter-ignore. | `skills/fsd-init/validate-config.py` (baru, menolak `credentials` di luar `docs/tasks/credentialRoles/` atau memuat `..`/absolut); `skills/fsd-doc/SKILL.md` §4.1 (path read/write tunggal + cek `.gitignore` path aktual). | Validator deterministik menolak path credential yang tidak aman (diverifikasi: `../secret.md` dan path absolut ditolak dengan pesan field). Aturan "satu path untuk baca DAN tulis" serta pengecekan `.gitignore` atas path aktual (bukan asumsi folder fixed) tetap bersifat instruksi yang ditegakkan agen — tidak ada eksekusi kode saat sesi `/fsd-doc` berjalan. |
| V2-007 | P1 | **Diperbaiki** (2026-08-09) — instruksi | Keamanan prompt/browser | Tidak ada trust boundary eksplisit untuk source code, config, template override, halaman browser, file unduhan, atau credential. Semua dibaca sebagai bagian discovery/login. | Instruksi terselubung dari repo atau halaman web dapat membelokkan agen saat mempunyai akses credential/browser, misalnya meminta upload secret atau navigasi ke origin lain. | `skills/fsd-doc/SKILL.md` — "Aturan #2 — Batas kepercayaan konten eksternal" (baru). | Ditambahkan aturan eksplisit: konten eksternal (kode/config/DOM/unduhan) adalah data bukan instruksi; larangan eksfiltrasi kredensial; batas navigasi browser ke origin `runtime.target_url`; wajib konfirmasi untuk aksi stateful/cross-origin. **Catatan kejujuran:** ini murni instruksi bagi agen LLM yang menjalankan skill — tidak ada penegakan kode/sandbox teknis (skill ini tidak mengeksekusi browser sendiri, hanya memanggil `agent-browser`), jadi efektivitasnya bergantung pada kepatuhan agen terhadap SKILL.md, bukan jaminan teknis yang tak bisa dilanggar. |
| V2-008 | P1 | **Diperbaiki** (2026-08-09) — instruksi | Integritas dokumen | Saat file FSD sudah ada, instruksi hanya mengatakan tambah menu sebagai BAB baru; tidak ada pemeriksaan eksplisit apakah menu/route/varian sudah ada. | Prompt `/fsd-doc <scope> "Dashboard"` yang diulang dapat menghasilkan BAB, prefix ID, aset, dan traceability ganda. | `skills/fsd-doc/SKILL.md` §"1. Tentukan target file". | Instruksi kini mewajibkan pencarian menu (via rute, bukan hanya label) di Peta Menu/sidecar sebelum menambah BAB: ditemukan → lanjutkan BAB yang ada (bukan duplikat); tidak ditemukan → BAB baru; ambigu → tanya dulu. Bersifat instruksi yang ditegakkan agen, sama seperti aturan lain di file ini (mis. Aturan #1) — tidak ada script yang memindai Peta Menu secara independen. |
| V2-009 | P1 | **Diperbaiki** (2026-08-09) — instruksi | Identitas role-set | `document_key` ditentukan oleh urutan `modules[]`. Mengurut ulang config dapat mengubah key file untuk set role yang sebenarnya sama. | `admin-ta,applicant` dapat sebelumnya menjadi `admin-ta--applicant`, lalu menjadi `applicant--admin-ta`; FSD lama tidak ditemukan dan dokumen scope duplikat dapat dibuat. | `skills/fsd-doc/SKILL.md` Langkah 0; `skills/fsd-convert/SKILL.md` Langkah 0; `README.md` (bagian "Mengapa urutan selector..."). | Urutan role-set kini leksikografis berdasarkan slug (stabil, tidak bergantung urutan mutable `modules[]`), dengan fallback eksplisit: bila dokumen tak ditemukan di key leksikografis tapi ada dokumen lama dengan key `modules[]`-order, agen wajib minta migrasi eksplisit, bukan menebak. Aturan urutan ditegakkan agen saat menghitung `document_key`; tidak ada kode yang menghitungnya secara independen dari instruksi ini. |
| V2-010 | P1 | **Diperbaiki** (2026-08-09) — validator | Validasi config | Keunikan slug, format URL-like, larangan `--`, bentuk path output, dan bentuk config hanya dinyatakan sebagai instruksi, tanpa validator deterministik sebelum artefak dibuat. | Config malformed atau slug bertabrakan dapat mengarahkan beberapa selector ke artefak yang sama atau menghasilkan path tidak valid. | `skills/fsd-init/validate-config.py` (baru); dipanggil dari `fsd-init` (langkah 5, saat config ditulis) dan opsional dari `fsd-doc`/`fsd-convert` Langkah 0. | Validator Python deterministik memeriksa: slug unik & URL-slug-like tanpa `--`, path `output.*_dir`/`credentials`/`docx.reference`/`brand.logo` relatif & aman (tanpa `..`/absolut), `docx_name_pattern` memuat `{Modul}`, warna brand `#RRGGBB`, `runtime.viewport` berformat `<w>x<h>`. Diverifikasi: config contoh lulus (`exit 0`); config rusak (12 pelanggaran sekaligus: slug ganda/format salah, portal invalid, credential path traversal, path absolut, pattern tanpa placeholder, warna 3-digit, viewport bukan `WxH`) terdeteksi semua dan `exit 1` tanpa menulis apa pun. Bila `fsd-init` tak terpasang bersisian, `fsd-doc`/`fsd-convert` degrade ke pemeriksaan manual (bukan gagal total) — bagian ini tetap bergantung instruksi. |
| V2-011 | P2 | **Diperbaiki** (2026-08-09) — kode | Generator reference DOCX | Warna brand diubah langsung menjadi RGB tanpa validasi format/karakter/panjang. | Nilai seperti `#FFF` menyebabkan traceback `ValueError`; reference DOCX lama dapat tertinggal tanpa error config yang mudah ditindaklanjuti. | `skills/fsd-convert/docx-kit/make-reference-docx.py` (`_is_valid_hex` + validasi di `load_brand()`). | Warna `brand.color_primary`/`color_primary_dark`/`color_accent` divalidasi format `#RRGGBB` sebelum dipakai; tidak valid → pesan manusiawi per field ke stderr dan `sys.exit(1)` **sebelum** `build()` pernah dipanggil, sehingga `reference.docx` lama tidak pernah ditimpa. Diverifikasi: `#FFF` gagal bersih (bukan traceback) dan checksum `reference.docx` terbukti tidak berubah (`md5sum` sama sebelum/sesudah); warna valid (`#005BAA`) tetap berhasil generate seperti biasa. |

## Catatan yang tidak dimasukkan sebagai bug mandiri

| Kandidat | Keputusan | Alasan |
|---|---|---|
| `output.docx_name_pattern` tidak dibaca langsung oleh `build-docx.ps1` | Bukan bug mandiri | `/fsd-convert` memang diperintahkan menghitung nama dari config dan meneruskannya lewat `-Out`. Tetap perlu regression test agar agen selalu menerapkannya. |
| Fallback nama output `fsd-*.md` | Sudah diperbaiki di working tree | `skills/fsd-convert/docx-kit/build-docx.ps1:59-67` kini menghapus awalan `fsd-` dan mengubah `--` menjadi `-Dan-`. Pastikan perubahan ini masuk commit/rilis dan diuji untuk format v2 serta legacy. |
| Output folder belum ada | Bukan bug | Uji audit menunjukkan Pandoc membuat parent directory output secara sukses pada environment saat ini. |

## Urutan perbaikan yang direkomendasikan

1. ~~**P0:** V2-001, V2-002, V2-003.~~ — **selesai (2026-08-09)**.
2. ~~**Keamanan sebelum screenshot/browser:** V2-006, V2-007.~~ — **selesai (2026-08-09)**.
3. ~~**Integritas lintas sesi dan prompt berulang:** V2-008, V2-009, V2-010.~~ — **selesai (2026-08-09)**.
4. ~~**Konsistensi branding dan developer experience:** V2-004, V2-005, V2-011.~~ — **selesai (2026-08-09)**.

Semua temuan V2-001..V2-011 sudah dikerjakan. Sisa pekerjaan: sesi verifikasi
langsung dengan agen sungguhan untuk item yang masih bersifat instruksi murni
(lihat kolom Status "Instruksi ditambahkan" di tabel regression test) — bukan
temuan baru, melainkan penguatan bukti untuk yang sudah ada.

## Regression test minimum sebelum rilis perbaikan

| Test | Ekspektasi | Status |
|---|---|---|
| Markdown dengan marker INTERNAL | Konversi menghapus seluruh blok atau berhenti dengan error yang jelas. | **Lulus** — diverifikasi manual 2026-08-09. |
| Markdown berisi ToC manual | Tidak ada ToC manual ganda di DOCX final. | Belum diuji (tidak berubah oleh perbaikan P0). |
| Gambar valid dan gambar hilang | Gambar valid ter-embed; referensi hilang menggagalkan konversi dan menyebut path yang salah. | **Lulus** — diverifikasi manual 2026-08-09. |
| `docx.reference` custom | Pandoc, hardener header, dan log memakai reference custom. | **Lulus (jalur validasi)** — diverifikasi path invalid ditolak; belum diuji end-to-end dengan reference custom valid. |
| `brand.logo` valid | Logo tampil di cover, atau config ditolak bila fitur dinonaktifkan. | **Lulus** — diverifikasi manual 2026-08-09 (logo ter-embed saat valid, exit 1 saat path salah). |
| Credential path `../secret.md` atau absolute path | Ditolak sebelum dibaca/ditulis. | **Lulus (validator)** — `validate-config.py` menolak keduanya; penegakan saat sesi `/fsd-doc` berjalan tetap instruksi (belum diuji end-to-end dengan agen sungguhan). |
| Browser redirect ke origin lain | Agen berhenti dan meminta konfirmasi; secret tidak dikirim. | **Instruksi ditambahkan** (Aturan #2) — tidak dapat diuji otomatis (bergantung kepatuhan agen); belum diverifikasi dengan sesi `agent-browser` sungguhan. |
| Prompt menu yang sama dua kali | BAB dan prefix tidak diduplikasi; proses melanjutkan BAB yang ada. | **Instruksi ditambahkan** — belum diverifikasi dengan sesi `/fsd-doc` sungguhan (perlu dua panggilan berurutan pada FSD nyata). |
| Reorder `modules[]` | Role-set tetap menemukan dokumen yang sama atau meminta migrasi eksplisit. | **Instruksi ditambahkan** (urutan leksikografis + fallback migrasi) — belum diverifikasi dengan sesi `/fsd-doc` sungguhan. |
| Config slug duplikat / memuat `--` / warna invalid | Preflight gagal dengan pesan per field tanpa menulis artefak. | **Lulus** — `validate-config.py` (slug/`--`/path/warna sekaligus, 12 pelanggaran terdeteksi, exit 1, tanpa menulis apa pun) DAN `make-reference-docx.py` (warna `#FFF` gagal bersih, `reference.docx` lama terbukti tidak berubah via checksum). |
